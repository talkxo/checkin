import type { Metadata } from 'next';
import SignupWizard from '@/components/beta-signup/signup-wizard';

// Isolated share-link page for the INSYDE beta program. Deliberately not
// linked from anywhere in the app — the URL is shared directly with
// organisations so they can hand over their setup details.

export const metadata: Metadata = {
  title: 'Sign up for the INSYDE Beta',
  description:
    'Tell us about your organisation and team — we use it to build your own INSYDE instance.',
};

export default function InsydeSignupPage() {
  return <SignupWizard />;
}
